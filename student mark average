Program: Find Average of Students

#include <stdio.h>

int main() {
    int i, s;
    float mark, total = 0, average;

    printf("Enter number of students: ");
    scanf("%d", &s);

    for(i = 1; i <= s; i++) {
        printf("Enter marks of student %d: ", i);
        scanf("%f", &mark);
        total = total + mark;
    }

    average = total / s;

    printf("\nTotal Marks = %.2f\n", total);
    printf("Average Marks = %.2f\n", average);

    return 0;
}

OUTPUT:
Enter number of students: 4
Enter marks of student 1: 70
Enter marks of student 2: 80
Enter marks of student 3: 60
Enter marks of student 4: 90

Total Marks = 300.00
Average Marks = 75.00
